using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RotateTrucks : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void RotateWheels(float xMov, float zMov)
    {
        // Vector3 direction = new Vector3(xMov, 0f, zMov);
        // if (direction != Vector3.zero)
        // {
        //     transform.rotation = Quaternion.Slerp(transform.rotation, Quaternion.LookRotation(direction), 10 * Time.deltaTime);
        // }
        // else
        //     transform.rotation = Quaternion.identity;
    }
}
